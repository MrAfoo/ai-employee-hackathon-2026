apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - serviceaccount.yaml
  - configmap.yaml
  - pvc.yaml
  - deployment.yaml
  - service.yaml
  - cronjob-daily.yaml
  - cronjob-weekly.yaml

commonLabels:
  app.kubernetes.io/name: reporting-agent
  app.kubernetes.io/component: ai-employee
  app.kubernetes.io/part-of: reporting-agent-system

namespace: default
