# ─────────────────────────────────────────────────────────────
#  Bronze Tier – .env.example
#  Copy to .env and fill in your real values.
#  NEVER commit .env to git.
# ─────────────────────────────────────────────────────────────

# ── Groq API (free – get key at https://console.groq.com) ────
GROQ_API_KEY=gsk_...
GROQ_MODEL=llama-3.3-70b-versatile

# ── Vault Path (absolute path to your Obsidian vault) ─────────
VAULT_PATH=./Vault

# ── Gmail (OAuth2 – NOT app password) ─────────────────────────
# Download credentials.json from Google Cloud Console:
# https://console.cloud.google.com → APIs → Gmail API → Credentials
GMAIL_CREDENTIALS_PATH=./credentials.json

# ── WhatsApp Business Cloud API ───────────────────────────────
# Full setup guide: BronzeTier/WHATSAPP_SETUP.md
# Get from: https://developers.facebook.com → Your App → WhatsApp → Getting Started
WHATSAPP_API_TOKEN=EAALKxxxxxxx...
WHATSAPP_PHONE_NUMBER_ID=1234567890123
WHATSAPP_BUSINESS_ACCOUNT_ID=1234567890
# Set any random string — use the same value in Meta Developer Portal webhook config
WHATSAPP_VERIFY_TOKEN=my_secure_verify_token_change_this
WHATSAPP_WEBHOOK_PORT=8002
WHATSAPP_MCP_PORT=8003
# Queue folder — webhook writes here, watcher reads from here
WHATSAPP_QUEUE_PATH=./whatsapp_queue

# ── Finance Watcher ───────────────────────────────────────────
FINANCE_DROP_PATH=./Vault/Finance_Drop
# Flag payments over this amount for human approval
APPROVAL_THRESHOLD_AMOUNT=500

# ── Watcher Intervals (seconds) ───────────────────────────────
WATCHER_POLL_INTERVAL=60

# ── Silver Tier MCP Email Server ──────────────────────────────
# Start with: python SilverTier/mcp_servers/email_mcp_server.py
EMAIL_MCP_URL=http://localhost:8001
EMAIL_MCP_PORT=8001

# ── Gmail SMTP (for MCP email server to send replies) ─────────
GMAIL_EMAIL=your-email@gmail.com
GMAIL_APP_PASSWORD=xxxx xxxx xxxx xxxx
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587

# ── Health Alerts ─────────────────────────────────────────────
HEALTH_ALERT_EMAIL=your-email@gmail.com
